{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "if": "Bash(git commit *)",
        "hooks": [
          {
            "type": "command",
            "command": "npm run build > /dev/null 2>&1 || echo '{\"continue\":false,\"stopReason\":\"Build failed! Fix build errors before committing.\"}'",
            "timeout": 120,
            "statusMessage": "Verifying build before commit..."
          }
        ]
      }
    ]
  }
}
